{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal capability for the RWANG local webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
